C:\Program Files\Java\jdk1.8.0_121\bin
Add to "Path" variabili di sistema per "javac"

Java is always pass-by-value. Unfortunately, they decided to call the location of an object a "reference".
 When we pass the value of an object, we are passing the reference to it.
https://stackoverflow.com/questions/40480/is-java-pass-by-reference-or-pass-by-value